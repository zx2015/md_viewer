# Code highlighting

Sample in multiple languages.

## Python

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"

print(greet("world"))
```

## JavaScript

```javascript
function greet(name) {
  return `Hello, ${name}!`;
}
console.log(greet("world"));
```

## Plain

```
no language specified
should still be wrapped in <pre>
```
